const usuario = 'Diego'; 
const idade = 23; 
console.log(`O usuário ${usuario} possui ${idade} anos`);

const name = 'Diego'; 
const age = 23; 
const user = { 
    name,
    age, 
    city: 'Rio do Sul', }
 
console.log(user);